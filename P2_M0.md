# P2-M0 环境验证清单（Phase 2 批量 RAW 开发的可行性检查）

> 目的：在动手写 Phase 2 编排器之前，确认本机 darktable 能力满足需求。
> 每步都有「命令/操作 → 预期 → 实际记录」。**全部跑完再决定 P2-M1 的字段映射方案**。
> 结果直接填在每项的「实际」栏，本文件随结果一起提交。
>
> 素材：`testpic/RAW/DSC00886.ARW` 等真实 ARW（或者用 `F:\PS_Process` 里任一 ARW 的拷贝，
> **原库只读，测试一律用拷贝**）。

## 0. 执行记录（2026-09-09，执行人 DSH）

**状态：部分完成。1/2/5 已实测，3/4/6 待补。**

| 步骤 | 状态 | 结论 |
|---|---|---|
| 1 安装 darktable | ✅ | **5.6.1**（winget `darktable.darktable`），lensfun **0.3.4** |
| 2 darktable-cli 转 JPG | ✅ | 成功，但**清单里的命令写法在 5.6 已失效**，见下 |
| 3 OpenCL/cltest | ⚠️ | `darktable-cltest` **跑 10 分钟未返回**（疑似卡在设备枚举），未取到结论 |
| 4 neural restore | ❌ | 需 GUI 交互，本次未验证 |
| 5 lensfun 覆盖 | 🟡 | 数据库字符串初查完成；**18-135 与 23/1.4 命中的是 Fujifilm 条目**，需逐镜头实测 |
| 6 XMP 侧车读取 | ❌ | 未验证 |

### 2 的实测细节（对 P2-M2 编排器至关重要）

```bash
# 清单原文（5.6 下会失败）：
darktable-cli in.ARW out.jpg --width 2048 --height 2048 --format jpg
#   失败原因：① 路径含反斜杠会被 darktable 的参数解析吞掉
#             （报错 `C:UsersIPPAppDataLocalTempp2testout.jpg'`）
#             ② `--format` 不是 5.6 的选项（warning: unknown option）

# 实际可用写法（路径必须正斜杠；第二参数是输出路径，扩展名由 --out-ext 决定）：
darktable-cli C:/path/in.ARW C:/path/out --width 2048 --height 2048 --out-ext jpg
```

- 实测耗时：**2048px JPG 约 2.6s/张**（首次冷启动含初始化更长）
- 导出结果校验（当前会话模型不支持读图，改用统计）：mean=135.8 / p50=144 / p95=198 /
  过曝 0.00% / 欠曝 0.11% / SCRFD 检出主体脸 17.0% → **画面正常，非全黑全绿** ✅
- 安装路径：`%LOCALAPPDATA%\Programs\darktable\bin\`（winget 用户级安装，不在 Program Files）

### 5 的实测发现：**镜头清单与 DESIGN §9.4 严重不符**

`pic_process scan F:\PS_Process` 全库 15979 个文件（8603 ARW）实测镜头分布：

| 镜头 | 张数 | DESIGN §9.4 是否列出 |
|---|---|---|
| FE 24mm F1.4 GM | 2569 | ❌ **未列出（占比最高）** |
| E 18-135mm F3.5-5.6 OSS | 1731 | ❌ **未列出** |
| Sigma 50mm F1.4 DG DN Art | 1518 | ✅ |
| Viltrox 23mm F1.4 E | 1397 | ❌ **未列出（第三方）** |
| E 70-350mm F4.5-6.3 G OSS | 1231 | ✅ |
| FE 24-70mm F2.8 GM II | 151 | ✅ |
| Sigma 70-200mm F2.8 DG DN OS Sports | 4 | ❌ 未列出 |
| 200-600G | **0** | ✅ 列出但库里根本没有 |

lensfun 数据库（56 个 XML）字符串初查：

| 镜头 | 初查 | 备注 |
|---|---|---|
| FE 24mm F1.4 GM | ✅ 命中 `mil-sony.xml` | |
| FE 24-70mm F2.8 GM II | ✅ 命中 `mil-sony.xml` | |
| E 70-350mm F4.5-6.3 G OSS | ✅ 命中 `mil-sony.xml` | |
| E 18-135mm | ⚠️ 命中 `mil-fujifilm.xml` | **疑似是富士 18-135，非索尼 E 口** |
| Viltrox 23mm F1.4 | ⚠️ 命中 `mil-fujifilm.xml` | **疑似是富士口，非索尼 E 口** |
| Sigma 50mm F1.4 DG DN | ❌ 未命中 | 与 §9.4 预测一致 → 需兜底链路 |
| Sigma 70-200mm F2.8 DG DN OS | ❌ 未命中 | 新增发现 |

> **结论：DESIGN §9.4 的镜头清单需要按实测重写**；兜底链路要覆盖的镜头从 1 支变成
> 至少 3 支（Sigma 50 Art、Sigma 70-200 DG DN，以及待确认的索尼口 Viltrox/18-135）。

---

## 1. 安装 darktable 5.x（Windows 官方构建）

- 下载：https://www.darktable.org/install/（Windows → 官方安装包）
- **预期**：安装成功；`darktable --version` 报 ≥ 5.0（neural restore 模块需要 5.0+）
- 版本号：**5.6.1**（`winget install --id darktable.darktable -e`）
- 安装日期：2026-09-09
- 附：Lensfun **0.3.4** / Exiv2 0.27.7 / 64 bit

## 2. darktable-cli 跑通一张 ARW → JPG（无侧车，出厂参数）

```bash
# 5.6 实际用法（见 §0：
#   - 路径必须用正斜杠，反斜杠会被参数解析吞掉
#   - 没有 --format，用 --out-ext
#   - 第二参数是输出路径，不是「必须目录」）
darktable-cli C:/tmp/p2test/in.ARW C:/tmp/p2test/out --width 2048 --height 2048 --out-ext jpg
```

- **预期**：生成 JPG，曝光/白平衡为出厂默认，画面正常（不是全黑/全绿）
- 实际：**成功** → `C:/Users/IPP/AppData/Local/Temp/p2test/out.jpg`（675 KB）
- 单张耗时：**约 2.6s**（2048px；冷启动首次更久）
- 画面校验：mean 135.8 / over 0.00% / 检出人脸 → 正常 ✅

## 3. darktable-cltest：OpenCL / GPU 状态

```bash
darktable-cltest
```

- **预期**：列出 GPU 设备并 `successfully initialized`；失败不影响 P2-M1，但决定批量出图速度
- 实际：**10 分钟未返回，主动终止**。疑似卡在 OpenCL 平台枚举（无独显/驱动缺失时的已知表现）。
  待在有 GPU 的机器上手动复跑确认；不阻塞 P2-M1（CPU 导出实测 2.6s/张已可用）。

## 4. neural restore 模块确认（darktable 5.0 新功能）

在 darktable GUI 里打开任一 ARW（拷贝），查 darkroom 右侧模块列表：

- [ ] `neural restore` 模块存在
- [ ] 模块内 ONNX 后端可用（模块提示需要下载模型文件时记录提示内容）
- [ ] 是否有 RAW 域（Bayer / scene-referred RawNIND）降噪选项——判断方法：
      模块输入选项若只有 "RGB / sRGB" 则是 JPEG/RGB 域；若有 Bayer 输入选项说明 RAW 域已合入。
      参考：[darktable 5.0 文档 neural restore](https://darktable-org.github.io/dtdocs/en/module-reference/utility-modules/shared/neural-restore/)、
      [PR #20854（Bayer 域 RawNIND）](https://github.com/darktable-org/darktable/pull/20854) 是否在你的版本 changelog 里
- 实际记录：**未验证**（需 GUI；5.6.1 比 5.0 新 6 个小版本，RawNIND 有可能已合入，待 GUI 确认）
- **若 neural restore 的 RAW 域不可用**：P2-M2 降噪走 darktable 传统 profiled 降噪（denoise (profiled)），效果仍可接受——不影响继续。

## 5. lensfun 覆盖实测（4 支镜头逐个验证）

> ⚠️ 镜头清单需按 §0 的实测结果重写（原清单 4 支里 200-600G 库里没有，
> 而占比最高的 24mm GM / 18-135 / Viltrox 23 未列入）。

在 darkroom 里选中 lens correction 模块，EXIF 匹配（camera make/model + lens 由照片自带）：

| 镜头 | 测试照片（含该镜头 EXIF 的 ARW 拷贝） | lens correction 模块显示 | 数据库初查 | 实际（GUI 待验） |
|---|---|---|---|---|
| FE 24mm F1.4 GM | 待从 `F:\PS_Process` 取（占比最高，2569 张） | ＿＿ | 命中 mil-sony.xml | ＿＿ |
| E 18-135mm F3.5-5.6 OSS | 待取 | ＿＿ | ⚠️ 疑似富士条目 | ＿＿ |
| Viltrox 23mm F1.4 E | 待取 | ＿＿ | ⚠️ 疑似富士口 | ＿＿ |
| Sigma 50mm F1.4 DG DN Art | 待取 | ＿＿ | **未命中** | ＿＿ |
| E 70-350mm F4.5-6.3 G OSS | testpic/RAW/DSC00886.ARW 附近 | ＿＿ | 命中 mil-sony.xml | ＿＿ |
| FE 24-70mm F2.8 GM II | testpic/RAW/DSC00886.ARW | ＿＿ | 命中 mil-sony.xml | ＿＿ |
| Sigma 70-200 F2.8 DG DN OS | 待取（仅 4 张） | ＿＿ | **未命中** | ＿＿ |
| 200-600G | **N/A**（库中 0 张） | — | — | — |

- 顺带确认：lensfun 版本 **0.3.4**（`darktable --version` 输出）；
  如需更新可跑 `lensfun-update-data` 后复查上述几条。

## 6. darktable 读 XMP 侧车验证（P2-M1 的前置）

Phase 1 先跑一份侧车（**用拷贝目录**，别动原库）：

```bash
mkdir F:\Claude\DSH\pic_process\p2_test
robocopy F:\PS_Process\<任一活动> F:\Claude\DSH\pic_process\p2_test *.ARW /MT:8
pic_process score F:\Claude\DSH\pic_process\p2_test --xmp
```

（注意：`--xmp` 目前只对有同名 JPG 的 ARW 写侧车；纯 ARW 目录不会产出侧车——
再从对应活动拷几张同名 JPG 一起进去，或直接用 testpic 布局验证。）

然后：

- [ ] darktable（GUI）导入该目录 → 星级（1-5）显示正确
- [ ] `firstcut:` 自定义子分在 darktable 的 XMP 元数据面板可见（预期：darktable 只读 `xmp:Rating`，自定义命名空间被保留但不显示为 UI）
- [ ] darktable 对该 ARW 做一次本地修改并保存侧车 → 确认它把 `firstcut:` 字段保留下来了（不丢）
- 实际记录：**未验证**（GUI 步骤）

## 7. 结论（跑完填）

- darktable 版本满足（≥5.0）：✅ **5.6.1**
- neural restore RAW 域可用：**待 GUI 确认**（❌ 则退回 profiled 降噪，不阻塞）
- lensfun 覆盖（数据库初查）：24mm GM ✅ · 24-70 GM II ✅ · 70-350 ✅ ·
  **18-135 ⚠️** · **Viltrox 23 ⚠️** · **Sigma 50 ❌** · **Sigma 70-200 ❌** · 200-600 N/A
- darktable-cli 吞吐：**2.6s/张**（2048px）→ 100 张约 **4.5 分钟**（单线程串行）
- **结论：P2-M1 侧车字段映射按什么方案做**：
  1) CLI 编排必须统一用**正斜杠路径**，导出参数用 `--out-ext`（不能照抄旧文档）；
  2) 镜头校正走 lensfun，命中的直接 auto；**Sigma 50 Art / Sigma 70-200 必走兜底**
     （RawTherapee 读索尼内嵌数据 或 跳过），Viltrox/18-135 待 GUI 实测后定；
  3) 降噪先按 profiled 降噪设计，neural restore 的 RAW 域确认后再升级。
