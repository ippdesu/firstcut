# firstcut — 版本说明

用 Rust 编写的本地照片初筛工具：扫描索尼相机 JPG+ARW 目录，五维评分（清晰度 / 曝光 / 噪点 / 构图 / 美学）+ 连拍去重，输出 CSV 报告与 XMP 星级侧车（Lightroom / darktable 均可读）。全程本地离线运行，照片不上传。

---

## 未发布（main，M6/M7/M8/M9/M-UI1 之后）

> 尚未打 tag；以下为相对 v1.1 的变更。

**✨ 新功能（M9 自适应连拍保留，默认开启）**
- **按姿势保留连拍**：SCRFD 5 关键点（双眼/鼻/双嘴角）解码为头部姿态描述子，
  dHash 子簇内再按姿势聚类，**每个姿势簇各自保留 top-3**（`-k` 可调）。
  30fps 连拍不同姿势不再被 dHash 判重丢掉；单组保留总量上限 20（超出按总分截断）。
- 阈值 0.25 经真实图库定标（棚拍同姿势 0.02~0.06 / 跨姿势 0.46~0.51 双峰）；
  开关/阈值/上限在 `[dedup]` 配置段（`adaptive_keep = false` 回退旧行为）。
- **M9 只改连拍三列**：五维分数与星级与之前逐张一致（archive 119 张回归验证）。
- 缓存行新增姿态描述子（`CACHE_VERSION` 13，旧缓存自动重建）。

**✨ 新功能（M-UI1 本地复核界面）**
- 新子命令 **`pic_process review <目录>`**：浏览器打开的本地复核界面（绑 127.0.0.1，只读）。
- 缩略图墙（分页懒加载，按需生成缩略图缓存）；筛选（星级/保留/五维下限/有人脸/搜索）与排序。
- **1:1 原图灯箱**：滚轮缩放 + 拖拽平移，`←/→` 同连拍组切换——缩略图看不清合焦时随时开原图。
- **连拍组并排对比**：勾选同组 2~4 张，多窗格**同步缩放平移**逐帧对比合焦位置。

**🧪 实验性**
- **`score --gpu`**（需 `cargo build --release --features gpu`）：DirectML 推理。
  实测 119 张 16.7s vs CPU 17.4s——无显著收益，保持实验性、默认构建不含。
- CSV 新增 `analysis_ok` 列（解码失败/无配对 ARW 可过滤），运行结束 stderr 输出失败清单。

**🐛 缺陷修复**
- **AI 预处理通道布局**：三模型输入通道错乱（已在 v1.1 修复）
- **EXIF Orientation 未应用**：竖拍照片人脸"躺着"，检测/构图系统性偏差；修复后人脸检出 75/119 → 109/119
- **EXIF Orientation 5/7 变换互换**：镜像+旋转的照片方向错误（5=转置、7=反对角线）
- **构图被贴纸脸/背景路人污染**：改为只统计主体级人脸（高度 ≥ 4%）
- **配对回归（M7 引入）**：`JPG/` + `RAW/` 分目录存放时 ARW 拿不到分数、不写侧车；改为"同目录优先 + 无歧义跨目录兜底"
- **同名文件跨目录互相覆盖**：索尼编号 9999 回绕导致上万张场景必然重名；配对键改为「目录 + 主干」
- **清晰度兜底缝隙**：SCRFD 只检出小脸（全部 <4%）时 pose 层被跳过，直接落到中性地板
- **连拍排序用默认权重**而非 `--config` 权重
- **换 `--config` 后缓存仍命中**：配置指纹已并入缓存键

**✨ 行为变更（可能影响既有用法）**
- **XMP 侧车命名**：`<stem>.<扩展名>.xmp` → **`<stem>.xmp`**（Lightroom 的约定，darktable 也读）。v1.1 之前写的侧车 Lightroom 读不到。
- **星级默认改为批次内相对分档**（前 10% 5★ / 10-30% 4★ / 30-65% 3★ / 65-90% 2★ / 后 10% 1★）。
  原因：绝对阈值下 119 张全部落在 4~5 星，星级失去筛选作用。想要绝对阈值设 `star_mode = "absolute"`。
  注意星级依赖批次——**建议整场照片一次跑完**；跨批次比较看 `total_score`。
- **配置解析 fail-fast**：`--config` 加载失败直接报错退出（不再回退默认值）；**未知字段名会被拒绝**
  （以前静默忽略，现在报错——手滑字段名不再静默失效）。
- **改权重/星级阈值不再触发全量重算**（配置指纹只覆盖曲线参数）；改 `sharpness_k`/`noise_k0`/`exposure_*` 才重算。
- **曝光模型**：从"偏离中灰的高斯"改为 **EV 容差带**（±1 档满分，-4/+2 档归零，两侧独立）+ 主体脸亮度单向修正。
- **场景预设**：`config-template --preset portrait|stage|highkey|sports|lowlight`。
- CSV 新增 `stars` 列。

**🧪 测试**：42 单元测试 + 6 集成测试全过（含 EXIF 8 种方向、配对四种布局、配置校验、指纹收窄、M9 姿态聚类/组上限/开关回退）。

---

## v1.1 — 修复合并版（AI 预处理通道布局 + 权重修正）

## v1.1 变更（相对 v1.0）

**🐛 修复：AI 预处理通道布局 bug**
- 人脸检测（SCRFD）、美学评分（CLIPIQA）、姿态检测（YOLOv8-pose）的预处理
  批量改写曾误将 RGB 交错数据按平面拆分，导致三模型输入通道错乱
  （实测美学分 15-25、人像 0 脸）
- 修复后与 v1.0 逐像素实现**输出完全一致**（逐张比对通过）

**⚖️ 权重修正**：默认权重 0.30/0.25/0.15/0.15/0.15（和 = 1.0）
- 曝光从 0.20 提升到 0.25（欠曝照片不再虚高），从清晰度挪 0.05 保持总和 1.0

**🧪 集成测试**：新增 6 项端到端测试；本地 testpic 缺失时自动跳过（克隆环境不再报错）

**🔧 工程改进**：二进制统一命名（`pic_process-*`）、base64 标准 crate 替代自写、版本号 1.0.0

## 功能

- **五维评分**（权重可配，`--config` 多场景配置 + 5 份内置场景预设）：
  - 清晰度：主体感知三层链路（SCRFD 人脸区域 → YOLOv8-pose 头部区域 → 中性兜底），大光圈浅景深照片不会被误判
  - 曝光：过曝/欠曝比例 + **EV 容差带**（±1 档满分，两侧独立）+ 主体脸亮度单向修正
  - 噪点：暗部平滑块 P15 + ISO 容忍度
  - 构图：主体级人脸三分法位置 + 大小 + 多人降权
  - 美学：CLIPIQA（CLIP 底座大模型）
- **连拍去重**：时间聚类 + dHash 感知哈希 → 子簇内排序，`-k` 控制每子簇保留数
- **XMP 星级侧车**（`--xmp`）：`<stem>.xmp`，Lightroom / darktable 可读；他人侧车不覆盖
- **SQLite 增量缓存**：重跑只处理新照片（秒级）
- **gallery 工具**：HTML 联系表（缩略图 + 分数）

## 使用

```bash
pic_process.exe config-template -o portrait.toml   # 生成评分配置模板
pic_process.exe score <照片目录> --xmp             # 评分 + XMP 星级
pic_process.exe score <照片目录> --config portrait.toml --cache portrait.sqlite
pic_process-gallery.exe report.csv -o gallery.html # HTML 联系表
```

## 模型准备（一次性，models/ 目录）

| 文件 | 来源 | 大小 |
|---|---|---|
| `clipiqa_model.onnx` + `.onnx.data` | hf-mirror.com/86Cao/IQA-ONNX-Models | ~153MB |
| `scrfd_10g_bnkps.onnx` | hf-mirror.com/RuteNL/SCRFD-face-detection-ONNX | 16.9MB |
| `yolov8n_pose.onnx` | hf-mirror.com/Xenova/yolov8n-pose | 13.5MB |

模型缺失时自动降级为纯像素评分；`--no-ai` 可显式跳过。

## 验证

- 37 单元测试 + 6 集成测试全过（v1.1 时为 12 + 6）
- 119 张 33MP 真实照片冷跑 ~18s（16 核）；修复后 AI 输出与 v1.0 逐张一致
