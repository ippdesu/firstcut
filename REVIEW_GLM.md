# 外部评审回应（GLM，2026-09-08）

> 原始评审全文见 `glm-review` 分支的 `GLM_REVIEW.md`（13 项，含位置/证据/修复方案/验收标准）。
> 本文件记录**逐条核实结论与处理状态**，避免评审文档与实现状态脱节。

**核实方式**：逐条实机复现（`cargo test`、对 `testpic`/`archive` 实跑、矩阵推演、值域采样），
不采信"看起来对"的结论。

## 结论总表

| 工单 | 问题 | 核实结论 | 状态 |
|---|---|---|---|
| PR-1 | 配对回归：`JPG/` + `RAW/` 分目录时 ARW 拿不到分数 | **成立（P0，M7-3 引入的回归）**；集成测试确实失败 | ✅ 已修（两步配对：同目录优先 + 无歧义跨目录兜底） |
| PR-2 | `--config` 失败只警告；未知字段静默忽略；`star_mode` 拼错静默回退 | **全部成立** | ✅ 已修（硬报错 + `deny_unknown_fields` + star_mode/百分位校验） |
| PR-3 | EXIF Orientation 5/7 变换互换 | **成立**（3×2 矩阵推演确认：原写法 5→反对角线、7→转置） | ✅ 已修 + 8 方向单元测试 |
| PR-4a | pose 兜底在"只有小脸"时被跳过 | **成立** | ✅ 已修（条件改为 `sharpness_region.is_none()`） |
| PR-4b | 区域分/全局分取 max 未入档 | **成立**（文档缺失，行为本身是设计取舍） | ✅ 已补文档（行为不变） |
| PR-4c | `reblur_mean_region` 实际返回 P80 | **成立** | ✅ 改名 `reblur_p80_region` |
| PR-5a | 配置指纹含权重/星级阈值 → 改权重触发全量重算 | **成立**（实测：改权重现在仍全命中） | ✅ 已修 |
| PR-5b | 缓存命中判定重复、`ScoreCache::get` 死代码 | **成立** | ✅ 已收敛为 `CacheRow::matches` |
| PR-5c | `flush` 每次重写全部行 | **成立** | ✅ 已改为只写 dirty 行 |
| PR-6 | 文档批次修正（测试数、权重注释、burst_size 语义、5% 门槛、几何精度） | **全部成立** | ✅ 已修 |
| PR-7 | pose 输出是否需 sigmoid 注释矛盾 | **成立（注释矛盾）；实测 248 个置信度全在 (0,1) → 已 sigmoid** | ✅ 只统一注释，代码不变 |

**附A（暂不实施）**：letterbox 预处理、清晰度"命中即用区域分"、相对星级小批次畸形、
他人侧车覆盖边界、低危备忘项——均记录在案，未动。

**附B（评审判定为正确的项）**：与本次核实一致，未发现误判。

## 由本次评审新增的待办

- [ ] Lightroom 真实导入验证（命名已对，但从未实测）
- [ ] 自适应连拍保留（`DESIGN.md` §7.1 设计草案）
- [ ] ground truth 校准（人工排序 vs 程序排序的相关性）
- [ ] letterbox 预处理（需配合人脸检出率回归，当前基线 109/119）

## 流程教训

PR-1 之所以能进 main：M7 提交时跑了 `cargo test` 但用 `Select-Object -First 3` **截断了输出**，
只看到 lib 测试结果就误判"全过"。此后测试结果必须看完整尾部，不能截断。
